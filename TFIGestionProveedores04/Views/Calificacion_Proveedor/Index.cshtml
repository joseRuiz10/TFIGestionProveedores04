@model IEnumerable<TFIGestionProveedores04.Calificacion_Proveedor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.comentario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Calificacion.idCalificacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Proveedor.RazonSocial)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.comentario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Calificacion.idCalificacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Proveedor.RazonSocial)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCalificacion_Proveedor }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCalificacion_Proveedor }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCalificacion_Proveedor })
        </td>
    </tr>
}

</table>
